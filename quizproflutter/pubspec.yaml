name: quizproflutter
description: "AVAlON - A professional Quiz & Assessment platform for BCA Final Year Project."
publish_to: 'none'

version: 1.0.0+1

environment:
  # Matches your current Dart SDK 3.10.7
  sdk: '>=3.10.0 <4.0.0' 

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^1.6.0
  provider: ^6.1.5+1
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true